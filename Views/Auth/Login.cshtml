@{
    Layout = null;
    ViewData["Title"] = "Login";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Sistema de Gestion de Consorcios</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body class="auth-page">
    <main class="auth-shell">
        <section class="auth-panel">
            <a class="brand-link" asp-area="" asp-controller="Home" asp-action="Index">
                <span class="brand-mark">GC</span>
                <span>
                    <strong>Gestion</strong>
                    <small>de Consorcios</small>
                </span>
            </a>

            <div class="auth-panel-content">
                <span class="sidebar-label">Acceso al sistema</span>
                <h1>Administracion simple para consorcios, propietarios y reservas.</h1>
                <p>
                    Ingreso con roles diferenciados para administracion y propietarios vinculados a unidades funcionales.
                </p>
            </div>

            <div class="role-card">
                <span class="status-dot"></span>
                <div>
                    <strong>Estado frontend</strong>
                    <small>Sesion conectada al backend</small>
                </div>
            </div>
        </section>

        <section class="auth-content">
            <div class="auth-card">
                <div class="auth-card-header">
                    <span class="status-badge success">Backend</span>
                    <h2>Ingresar</h2>
                    <p>Usa un email de propietario asociado a una UF o ingresá como administrador.</p>
                </div>

                @if (ViewBag.Error is string errorMessage)
                {
                    <div class="form-alert show" role="alert">@errorMessage</div>
                }

                <form class="auth-form" asp-controller="Auth" asp-action="Login" method="post" data-login-form data-real-submit novalidate>
                    @Html.AntiForgeryToken()
                    <div>
                        <label class="form-label" for="email">Email</label>
                        <input class="form-control" id="email" name="email" type="email" placeholder="usuario@consorcio.com" autocomplete="email" required />
                        <span class="field-validation" data-error-for="email"></span>
                    </div>

                    <div>
                        <label class="form-label" for="password">Contrasena</label>
                        <input class="form-control" id="password" name="password" type="password" placeholder="Ingresar contrasena" autocomplete="current-password" required />
                        <span class="field-validation" data-error-for="password"></span>
                    </div>

                    <div>
                        <label class="form-label" for="role">Rol</label>
                        <select class="form-select" id="role" name="role" required>
                            <option value="">Seleccionar rol</option>
                            <option value="Administrador">Administrador</option>
                            <option value="Propietario">Propietario</option>
                        </select>
                        <span class="field-validation" data-error-for="role"></span>
                    </div>

                    <button class="btn btn-primary w-100" type="submit">Ingresar al panel</button>
                </form>

                <div class="auth-helper">
                    <span>El rol propietario valida email activo y UF asociada.</span>
                    <a asp-area="" asp-controller="Home" asp-action="Index">Volver al inicio</a>
                </div>
            </div>
        </section>
    </main>

    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
</body>
</html>
